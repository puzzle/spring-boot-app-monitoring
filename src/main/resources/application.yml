
management:
  endpoints:
    web:
      exposure:
        include: "*"
      base-path: "/api"
  endpoint:
    prometheus:
      enabled: true
  metrics:
    export:
      prometheus:
        enabled: true
